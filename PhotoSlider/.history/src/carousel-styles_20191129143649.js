import config from './config-styles.js'

export default {
  root: {
  //  position: 'relative',
   // overflow: 'hidden',
   // height: config.imageHeight
   flex: 1,
   alignSelf: 'stretch',
   width: win.width,
   height: win.height,
  }
}
